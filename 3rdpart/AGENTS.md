# AGENTS.md

This directory is for third-party C++ source, prebuilt binaries, patches, license files, and CMake glue.

- Record source, version, license, and local patch rationale for every dependency added here.
- Keep vendored source in `cpp/`, prebuilt binaries in `prebuilt/`, patches in `patches/`, licenses in `licenses/`, and CMake integration in `cmake/`.
- Do not add investigation data or generated analysis artifacts here.
